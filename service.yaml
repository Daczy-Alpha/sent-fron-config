apiVersion: v1
kind: Service
metadata:
  name: sentiment-frontend-service
  namespace: sentiment-app

spec:
  type: NodePort

  selector:
    app: sentiment-frontend

  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080
